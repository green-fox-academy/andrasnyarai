current_hours = 14;
current_minutes = 34;
current_seconds = 42;

# Write a program that prints the remaining seconds (as an integer) from a
# day if the current time is represented bt the variables
hours = 24
minutes = 60
seconds = 60



print((((hours - current_hours)*60)*60) + ((minutes - current_minutes)*60) + (seconds - current_seconds))